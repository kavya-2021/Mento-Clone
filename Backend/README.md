# mento-server-deployment
This is the node js app, built as server(backend) for the project clone of website "Mento", to provide data and to enable interactions with it whenever required.

This Node  app has created using express, mongoose, mongoDB atlas. And added all the CRUD queries to interact with the server.
This app is used as server or backend for the Project clone of website  **Mento** ..
